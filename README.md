# Simple BlackJack Game (front side, based on React)

Use NodeJS >= 16

# Run:
npm install
npm start

# Note:
Edit the file /src/App/Server/Server.js and fix the API link if you want to refer to a local API server.

